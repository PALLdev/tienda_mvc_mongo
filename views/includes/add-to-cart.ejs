<form action="/cart" method="POST">
    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
    <input type="hidden" name="idProducto" value="<%= product._id %>">
    <button class="btn" type="submit">Añadir al carro</button>
</form>